============
Installation
============

At the command line::

    $ easy_install django-fake-model

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-fake-model
    $ pip install django-fake-model
